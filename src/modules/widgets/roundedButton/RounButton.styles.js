import {StyleSheet} from 'react-native';
import {colors, Fonts} from '../../theme/css/Common';

const styles = StyleSheet.create({
  container: {
    width: 150,
    borderRadius: 30,
    justifyContent: 'center',
    backgroundColor: colors._1977F3,
    height: 50,
  },
  nextText: {
    fontSize: 17,
    textAlign: 'center',
    color: colors._F9F8FF,
  },
});

/* export the styling */
export default styles;